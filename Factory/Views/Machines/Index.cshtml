@{
  Layout ="_Layout";
}
 @using Factory.Models;
<h1>List Of Machines</h1>

@if(Model.Count ==0)
{
  <h3> No Machines has been Registered</h3>
}
<ul>
  @foreach(Machine machine in Model)
  {
    <li>@Html.ActionLink($"{machine.Name}", "Details",new{machine =machine.MachineId})</li>
  }
</ul> 
<h5>@Html.ActionLink("Add a new Machine","Create")</h5>
<h5>@Html.ActionLink("Home","Index","Home")</h5>